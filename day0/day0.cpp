//
// Created by nathan on 01-12-23.
//
#include "stdio.h"
#include <fstream>
#include <sstream>
#include <vector>
void runpart1() {
    std::ifstream infile("../day/example.txt");
    std::string line;
    int result = 0;
    // parsing
    while (std::getline(infile, line)){


    }

    std::cout<<result<<std::endl;
}
void runpart2() {
    std::ifstream infile("../day/example.txt");
    std::string line;
    int result = 0;
    // parsing
    while (std::getline(infile, line)){


    }

    std::cout<<result<<std::endl;
}